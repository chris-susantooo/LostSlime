import Trait from "../Trait.js";

export default class Collider extends Trait {
    
    constructor() {
        super('collider');
    }

    update(entity, deltaTime) {
        //if (entity.pos.y) 
    }
}